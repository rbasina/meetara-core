# Git LFS Configuration
# Large files (>100MB) should use Git LFS

# Track compressed data files via Git LFS
*.zip filter=lfs diff=lfs merge=lfs -text

# Specifically track large compressed directories
vectorstore.zip filter=lfs diff=lfs merge=lfs -text
images.zip filter=lfs diff=lfs merge=lfs -text

